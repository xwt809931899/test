<!--pages/main/index.wxml-->
<!-- <view class="jumpPage" bindtap='bindJumpTap'>跳转一下</view> -->
<!-- <button
  type="default"
  size="{{defaultSize}}"
  loading="{{loading}}"
  plain="{{plain}}"
  disabled="{{disabled}}"
  bindtap="default"
  hover-class="other-button-hover"
>
  default
</button> -->
<!-- <view class="page">
  <view class="page__hd">
    <text class="page__title">image</text>
    <text class="page__desc">图片</text>
  </view>
  <view class="page__bd">
    <view class="section section_gap" wx:for="{{array}}" wx:for-item="item">
      <view class="section__title">{{item.text}}</view>
      <view class="section__ctn">
        <image
          style="width: 200px; height: 200px; background-color: #eeeeee;"
          mode="{{item.mode}}"
          src="{{src}}"
        ></image>
      </view>
    </view>
  </view>
</view>
<button
  type="primary"
  size="{{primarySize}}"
  loading="{{loading}}"
  plain="{{plain}}"
  disabled="{{disabled}}"
  bindtap="bindJumpTap"
>
  跳转一下
</button> -->
<!-- <button
  type="warn"
  size="{{warnSize}}"
  loading="{{loading}}"
  plain="{{plain}}"
  disabled="{{disabled}}"
  bindtap="warn"
>
  警告
</button> -->

<view class="container">
  <!-- <view class="test"> -->
    <!-- <ul>
      <li>{{people[0].name}}</li>  小学生代码
      <li>{{people[1].name}}</li>
      <li>{{people[2].name}}</li>
    </ul> -->
    <!-- <ul>
      <li wx:for = "{{people}}" wx:key="{{index}}">{{item.name}}</li>   唯一标识
    </ul> -->
  <!-- </view> -->
 </view>
<swiper
  indicator-dots="true"
  indicator-color="#ffffff"
  autoplay="{{autoplay}}"
  interval="{{interval}}"
  duration="{{duration}}"
>
  <block wx:for="{{imgUrls}}" wx:key="{{index}}">
    <swiper-item>
      <image src="{{item}}" class="slide-image" width="355" height="150" />
    </swiper-item>
  </block>
</swiper>

